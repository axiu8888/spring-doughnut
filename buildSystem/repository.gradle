
buildscript {
    repositories {
        maven { url 'http://maven.aliyun.com/nexus/content/groups/public/' }
        mavenCentral()
        jcenter()
        maven { url 'https://repo.spring.io/milestone' }
        maven {
            url = "http://${repositoryAddress}/repository/nexus3Group/"
            credentials {
                username "${repositoryUserName}"
                password "${repositoryPassword}"
            }
        }
    }
}


repositories {
    maven { url 'http://maven.aliyun.com/nexus/content/groups/public/' }
    mavenCentral()
    jcenter()
    maven { url 'https://repo.spring.io/milestone' }
    maven {
        url = "http://${repositoryAddress}/repository/nexus3Group/"
        credentials {
            username "${repositoryUserName}"
            password "${repositoryPassword}"
        }
    }
}
