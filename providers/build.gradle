subprojects {

  apply from: "${getRootDir()}/buildSystem/base.gradle"

  group = "com.benefitj.provider"
  // 更新 groupId
  ext.groupId = "${group}"

  dependencies {
    compileOnly project(":all")
  }

}