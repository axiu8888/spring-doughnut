subprojects {

  apply plugin: 'java'

  group = "com.benefitj.services"
  // 更新 groupId
  ext.groupId = "${group}"

  dependencies {
    implementation(project(":commons"))
    compileOnly project(":all")
  }

}